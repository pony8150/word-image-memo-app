## Active Files

This directory is the working source for the app's word books.

Files currently used by backend scripts:
- `初中词汇.json`
- `高中词汇.json`
- `红宝书-考研词汇.json`

Notes:
- `*.backup-before-examples-*.json` files are temporary safety copies created by `backend/src/scripts/fill-missing-examples.ts`.
- `*-common-words.txt` and `school-common-words.*` are legacy export artifacts from old data-prep scripts and are not part of the current import flow.
